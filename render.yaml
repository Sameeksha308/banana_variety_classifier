services:
  - type: web
    name: banana-flask-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn flutter:app  # Correct command to start the Flask app
    envVars:
      - key: FLASK_ENV
        value: production

