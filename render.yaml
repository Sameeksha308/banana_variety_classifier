services:
  - type: web
    name: banana-flask-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --listen=0.0.0.0:5000 flutter:app  # Using waitress for deployment
    envVars:
      - key: FLASK_ENV
        value: production
