services:
  - type: web
    name: banana-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python flutter.py
    envVars:
      - key: FLASK_ENV
        value: production
